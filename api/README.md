### Как запустить генеератор Swagger'а

В корне проекта выполнить команду, в директории /api появится swagger файл
```
swag init --parseInternal --pd --dir cmd/myapp/,delivery/handler/ --output api/
```
